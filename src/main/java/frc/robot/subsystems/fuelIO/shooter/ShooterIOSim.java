package frc.robot.subsystems.fuelIO.shooter;

public class ShooterIOSim implements ShooterIO {
    // idk if I should even write this or if I can just do something in one of the sim commands
    // because like yes, I could simulate the motor and I guess use that rotations to inform how far the ball goes
    // we'll see if I bother
}
